اعمل داخل مشروع POS. تحويل الفئات لنظام ديناميكي + إزالة بيانات

تجريبية. كل قسم = commit. ثوابت: RTL logical CSS، النحاسي

#CF694A، المبالغ integer، العملة "د.أ".

M1 — حذف دالة البذر التجريبية

- src/db/queries/products.ts: احذف بالكامل دالة

  seedProductsIfEmpty (هي بيانات تجريبية وهمية)

- src/modules/pos/POSPage.tsx: احذف استيراد seedProductsIfEmpty

  واستدعاءه في useEffect

- النتيجة: المنتجات تأتي حصراً من صفحة المنتجات (إضافة صنف)

- ملاحظة: المنتجات الوهمية الأربعة الموجودة حالياً في القاعدة

  يحذفها المستخدم يدوياً من صفحة المنتجات لاحقاً — لا تحذفها

  بالكود (قد تكون مرتبطة بفواتير)

M2 — توحيد رمز العملة

- شاشة إضافة المنتج تعرض "د.أ" أصلاً (لا تغيّرها)

- ابحث في كامل المشروع عن أي "د.ع" متبقّية وصحّحها إلى "د.أ"

- إن لم توجد أي "د.ع"، تجاوز هذا القسم

M3 — جدول الفئات (هجرة 007)

أنشئ ملف الهجرة src/db/migrations/007_categories.sql وسجّله

في migrations/index.ts:

- أنشئ جدول categories:

  id TEXT PK, name TEXT NOT NULL, color TEXT NOT NULL DEFAULT

  '#CF694A', icon TEXT DEFAULT 'Box', sort_order INTEGER NOT

  NULL DEFAULT 0, is_active INTEGER NOT NULL DEFAULT 1,

  created_at TEXT NOT NULL

- أدرج الفئات الست الحالية كبذرة (id = القيمة الحالية نفسها):

  device='أجهزة' #2563EB، sim='شرائح' #7C3AED،

  service_general='خدمات عامة' #0D9488،

  service_repair='خدمات صيانة' #EA7317،

  accessory='إكسسوار' #D9A404، package='باقات' #DB2777

- أعد بناء جدول products لإزالة قيد CHECK على عمود category:

  اتبع نمط إعادة بناء الجداول في 002_remove_debts.sql

  ⚠️ مهم: افحص المخطط الحالي الفعلي لجدول products بعد كل

  الهجرات (001→006) واحتفظ بكل أعمدته دون استثناء — بما فيها

  image_path وicon وcost_price (المضاف في 005) — وأبقِ

  category نوعه TEXT NOT NULL بلا CHECK

- تحقّق أن كل بيانات المنتجات تُنقل سليمة بلا فقدان



M4 — استعلامات الفئات + شاشة الإدارة

- أنشئ src/db/queries/categories.ts:

  getCategories(activeOnly?) , addCategory, updateCategory,

  deleteCategory

- deleteCategory: امنع حذف فئة مرتبطة بمنتجات؛ بدلاً من ذلك

  أوقفها (is_active=0) أو اطلب نقل منتجاتها أولاً، برسالة واضحة

- أضف شاشة "إدارة الفئات" داخل الإعدادات (محمية بصلاحية مشرف):

  قائمة الفئات، إضافة فئة (اسم + لون + أيقونة + ترتيب)،

  تعديل، إيقاف/حذف — بنظام التصميم الحالي والهوية النحاسية

M5 — ربط الواجهات بالفئات الديناميكية

استبدل مصفوفات CATEGORIES الثابتة الثلاث بقراءة من getCategories:

- ProductGrid.tsx: كتل الفئات الملوّنة تُبنى من جدول categories

  (اللون من category.color)؛ تبقى كتلة "الكل" خاصة ثابتة

  (نحاسية). احذف خريطة categoryColors الثابتة واشتق تظليل

  بطاقة المنتج من لون الفئة

- ProductsPage.tsx: فلتر الفئات يُبنى من getCategories

- ProductEditor.tsx: قائمة <select> التصنيف تُبنى من getCategories

- IconPicker: إن كان يفلتر الأيقونات حسب الفئة، اجعله يتعامل

  بأمان مع الفئات الجديدة (افتراضي عام لو لا تطابق)

- استخدم queryKey ثابتاً لـ ['categories'] وأبطله بعد أي

  تعديل في شاشة إدارة الفئات

معايير القبول:

- [ ] npx tsc --noEmit → 0 أخطاء

- [ ] الهجرة تُطبَّق وتنقل بيانات المنتجات سليمة

- [ ] لا أثر لـ seedProductsIfEmpty

- [ ] رمز العملة "د.أ" في كل مكان

- [ ] إضافة فئة جديدة من الإعدادات تظهر فوراً في كتل POS

  وفي فلتر المنتجات وفي قائمة محرّر المنتج

- [ ] المنتجات الموجودة تبقى مرتبطة بفئاتها

بعد كل قسم commit، وبعد M5: git push origin main